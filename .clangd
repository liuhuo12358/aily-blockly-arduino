---
# clangd配置文件，用于过滤不需要的符号和改善代码补全体验

# 诊断配置
Diagnostics:
  # 禁用一些不必要的警告
  Suppress: 
    - "unused-parameter"
    - "unknown-pragmas"
    - "macro-redefined"
    - "deprecated-declarations"
  
  # 关闭clang-tidy的一些检查
  ClangTidy:
    Remove: 
      - "readability-*"
      - "google-*"
      - "modernize-*"
      - "cppcoreguidelines-*"
      - "hicpp-*"

# 索引配置
Index:
  # 限制索引的文件类型
  Background: Skip
  
  # 排除一些不需要的目录
  StandardLibrary: No

# 补全配置  
Completion:
  # 限制补全结果
  AllScopes: false
  
  # 不显示私有成员（以下划线开头的）
  IncludeIneligible: false

# 悬停信息配置
Hover:
  # 显示简洁的悬停信息
  ShowAKA: false

# 编译标志
CompileFlags:
  # 移除一些不需要的包含路径
  Remove: 
    - "-I**/avr/include"
    - "-I**/gcc/avr/**"
    - "-I**/lib/gcc/**"
  
  # 添加一些宏定义来隐藏内部实现
  Add: 
    - "-DARDUINO_INTERNAL_HIDE"
    - "-D__CLANGD_COMPLETION__"
